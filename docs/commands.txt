apt update
apt install -y mysql-server
systemctl enable --now mysql
mysql_secure_installation

-- if DB should be created by the app:
CREATE USER 'devuser'@'localhost' IDENTIFIED BY 'StrongPasswordHere';
GRANT CREATE, ALTER, DROP, INDEX, REFERENCES, SELECT, INSERT, UPDATE, DELETE, CREATE TEMPORARY TABLES, LOCK TABLES
ON *.* TO 'devuser'@'localhost';

mysql -h 127.0.0.1 -u devuser -p

~/working/court-diary-00/backend$ --> under this
mysql -u devuser -p < data/1_cs_schema_create.sql
mysql -u devuser -p < data/2_cs_data_inserts.sql

mysql -u devuser -p courtdiary < courtdiary_full.sql

sudo systemctl enable api.casdia.service
sudo systemctl daemon-reload
sudo systemctl start api.casdia.service
sudo systemctl restart api.casdia.service
journalctl -u api.casdia.service -n 50 --no-pager


sudo ln -s /etc/nginx/sites-available/casdiaapi.nyainfo.com /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

certbot certonly --manual --preferred-challenge dns -d "nyainfo.com" -d "*.nyainfo.com"


React:

sudo ln -s /etc/nginx/sites-available/court-diary.nyainfo.com /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

ssh root@srv1219119.hstgr.cloud

Frontend
su - myuser
cd working/courtease-ui-e1fa5329/
git pull
npm install
npm run build
exit
sudo systemctl restart nginx

Backend
su - myuser
cd working/court-diary-00/
git pull
cd backend
poetry install
exit
sudo systemctl restart api.casdia.service
# To check logs
journalctl -u api.casdia.service -n 50 --no-pager

# run DB
su - myuser
cd working/court-diary-00/backend/
mysql -u devuser -p < data/1_cs_schema_create.sql
mysql -u devuser -p < data/001_create_uuid_v7_function.sql
mysql -u devuser -p < data/002_create_uuid_triggers.sql
mysql -u devuser -p < data/2_cs_data_inserts.sql
